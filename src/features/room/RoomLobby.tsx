import type { RoomState } from '../../api/types'
import { Avatar, Button, Card, SpinnerText } from '../../components/ui'

export function PlayerList({ state }: { state: RoomState }) { return <Card><div className="sectionHead"><strong>플레이어</strong><span>{state.players.length}명</span></div><div className="players">{state.players.map(player => <div className="player" key={player.playerId}><Avatar name={player.nickname} gender={player.gender} /><span><b>{player.nickname}</b>{player.playerId === state.me.playerId && <small> 나</small>}</span>{player.host && <i>👑 방장</i>}{player.connectionStatus === 'DISCONNECTED' && <em>연결 끊김</em>}<span className={`online ${player.connectionStatus.toLowerCase()}`} /></div>)}</div></Card> }
export function RoomLobby({ state, onSelect }: { state: RoomState; onSelect: () => void }) { return <><div className="invite"><p>방 코드</p><button onClick={() => navigator.clipboard?.writeText(state.room.roomCode)}>{state.room.roomCode} <span>▣</span></button><small>코드를 눌러 복사하세요</small></div><div className="mascot small" aria-hidden><span>🐱</span><span>🐰</span><span>🐧</span></div><h1>{state.players.length}명이 모였어요!</h1><p className="sub">친구들이 모두 들어왔나요?</p><PlayerList state={state} />{state.me.host ? <Button onClick={onSelect}>게임 선택하기 <span>→</span></Button> : <SpinnerText>방장이 게임을 고르는 중이에요</SpinnerText>}</> }
