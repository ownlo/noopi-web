# AGENTS.md --- NOOPI Frontend

## 1. 역할

이 저장소는 **NOOPI(누피)** 모바일 웹 Frontend다.

NOOPI는 여럿이 같은 공간에 모였을 때 QR 또는 Room Code로 바로 참가하여
함께 즐기는 실시간 멀티플레이 게임 웹서비스다.

핵심 원칙:

> **게임은 사람끼리, 진행은 웹이.**

Frontend는 게임의 진실을 결정하지 않는다.

Frontend의 책임은 다음 세 가지다.

1.  Backend의 현재 상태를 정확하게 보여준다.
2.  현재 Player가 지금 수행할 수 있는 행동만 제공한다.
3.  사용자의 행동을 Backend에 정확하게 전달한다.

------------------------------------------------------------------------

## 2. Source of Truth

구현 전에 반드시 다음 문서를 읽고 따른다.

``` text
SERVICE_SPEC.md
GAME_SESSION_SPEC.md
REALTIME_SPEC.md
API_SPEC.md
UI_SPEC.md
FRONTEND_ARCHITECTURE.md
games/LIAR_GAME_SPEC.md
games/BLIND_GAME_SPEC.md
```

우선순위:

``` text
게임 규칙
→ games/LIAR_GAME_SPEC.md
→ games/BLIND_GAME_SPEC.md

REST / WebSocket 계약
→ API_SPEC.md

화면 / UX
→ UI_SPEC.md

Frontend 기술 구조
→ FRONTEND_ARCHITECTURE.md

Room / GameSession 공통 개념
→ SERVICE_SPEC.md
→ GAME_SESSION_SPEC.md
→ REALTIME_SPEC.md
```

문서 간 충돌을 발견하면 임의로 한쪽을 선택하여 구현하지 않는다.

충돌 내용을 명확하게 보고하고 결정을 요청한다.

------------------------------------------------------------------------

## 3. SPEC에 없는 기능을 만들지 않는다

SPEC에 정의되지 않은 기능을 임의로 추가하지 않는다.

특히 다음 기능을 추가하지 않는다.

``` text
회원가입
로그인
친구
채팅
음성 채팅
프로필
랭킹
업적
아이템
상점
복잡한 점수
게임 히스토리
전적
```

"있으면 좋아 보인다"는 이유로 기능을 추가하지 않는다.

------------------------------------------------------------------------

## 4. 기술 스택

기본 기술 스택:

``` text
Vite
React
TypeScript
React Router
TanStack Query
WebSocket
CSS Modules 또는 일반 CSS
ESLint
Prettier
```

Zustand는 필요한 Client-only UI State가 있을 때만 사용한다.

Next.js로 변경하지 않는다.

Redux를 추가하지 않는다.

다른 대형 상태관리 라이브러리를 임의로 추가하지 않는다.

새 Dependency를 추가하기 전에 기존 기술로 해결 가능한지 먼저 확인한다.

------------------------------------------------------------------------

## 5. 모바일 우선

NOOPI는 Mobile First다.

주 사용 환경:

``` text
스마트폰 세로 화면
```

기본 콘텐츠 영역은 다음을 권장한다.

``` text
width: 100%
max-width: 430px
```

PC에서는 모바일 레이아웃을 중앙에 배치할 수 있다.

가로 스크롤이 발생하지 않게 한다.

핵심 버튼은 터치하기 충분한 크기로 만든다.

------------------------------------------------------------------------

## 6. 디자인 방향

기본 분위기:

``` text
Dark Navy
Neon Purple / Blue
둥근 카드
큰 버튼
가볍고 장난스러운 게임 분위기
귀여운 캐릭터 / 이모지 활용 가능
```

과도한 장식보다 게임 진행 상태와 현재 행동의 명확성을 우선한다.

게임 중 불필요한 메뉴와 네비게이션을 추가하지 않는다.

------------------------------------------------------------------------

## 7. 프로젝트 구조

기본 구조를 유지한다.

``` text
src/
├── app/
├── pages/
├── features/
│   ├── client/
│   ├── room/
│   ├── game-session/
│   └── games/
│       └── liar/
├── components/
├── api/
├── realtime/
├── hooks/
├── store/
├── mocks/
├── styles/
└── utils/
```

새 게임은:

``` text
features/games/{game}
```

아래에 추가한다.

Room 코드 안에 특정 게임의 규칙을 넣지 않는다.

------------------------------------------------------------------------

## 8. Room과 Game 분리

Room은 사람들이 모여 있는 공간이다.

GameSession은 Room 안에서 진행되는 한 판이다.

Room은 라이어 게임의 내부 규칙을 알면 안 된다.

금지 예:

``` ts
// room feature 내부
if (gameType === 'LIAR') {
  calculateLiarWinner()
}
```

게임별 화면과 표현은 해당 게임 Feature에서 처리한다.

------------------------------------------------------------------------

## 9. Backend가 Source of Truth

다음은 Backend가 결정한다.

``` text
Room 상태
Player 목록
Host
GameSession
게임 phase
역할
제시어
첫 발언자
투표 후보
투표 완료 상태
투표 집계
재투표 여부
최종 지목자
라이어 판정
제시어 정답 판정
승패
블라인드 게임의 Player별 제시어 배정
블라인드 게임의 정답 판정과 승자
```

Frontend에서 이 값을 자체 계산하여 확정하지 않는다.

------------------------------------------------------------------------

## 10. Server State 중복 저장 금지

Server State는 TanStack Query를 기준으로 관리한다.

다음 데이터를 Zustand 또는 별도 Global Store에 복제하지 않는다.

``` text
Room
Players
GameSession
GameState
Vote Result
Winner
```

두 개의 Source of Truth를 만들지 않는다.

------------------------------------------------------------------------

## 11. Client State

Frontend가 소유할 수 있는 상태:

``` text
clientId
입력 중인 nickname
선택 중인 gender
아직 제출하지 않은 투표 선택값
모달
Toast
기타 일시적 UI 상태
```

서버에서 다시 복구해야 하는 게임 정보를 Client State로 확정하지 않는다.

------------------------------------------------------------------------

## 12. clientId

로그인이 없으므로 익명 `clientId`를 사용한다.

키:

``` text
noopi.clientId
```

없으면:

``` ts
crypto.randomUUID()
```

등으로 생성한다.

Room 관련 요청에는:

``` text
X-Client-Id
```

헤더를 전달한다.

`clientId`를 실제 인증 토큰으로 간주하지 않는다.

------------------------------------------------------------------------

## 13. localStorage 제한

저장 가능:

``` text
noopi.clientId
noopi.lastRoomId
```

필요한 경우 UX용 마지막 nickname/gender 정도는 저장할 수 있다.

신뢰 가능한 게임 상태로 다음을 저장하지 않는다.

``` text
role
keyword
game phase
vote result
winner
다른 Player 정보
```

새로고침 후 서버 `/state`에서 복구한다.

------------------------------------------------------------------------

## 14. Room Route

게임 phase별 URL을 만들지 않는다.

기본:

``` text
/rooms/:roomId
```

Room 화면 안에서 현재 GameSession과 phase를 기준으로 View를 선택한다.

다음과 같은 구조는 만들지 않는다.

``` text
/rooms/:id/role
/rooms/:id/vote
/rooms/:id/result
```

서버 상태와 URL 상태가 서로 어긋나는 구조를 피한다.

------------------------------------------------------------------------

## 15. 현재 Player가 할 수 있는 행동만 보여준다

가장 중요한 UI 규칙이다.

예:

``` text
Host
→ 게임 선택 O

일반 Player
→ 게임 선택 X
→ 기다림 UI
```

``` text
투표 전
→ 투표 UI O

투표 완료
→ 투표 UI X
→ 기다림 UI
```

``` text
LIAR_GUESS + LIAR
→ 정답 입력 O

LIAR_GUESS + CITIZEN
→ 정답 입력 X
→ 기다림 UI
```

불가능한 행동을 disabled 버튼으로 계속 노출하기보다 필요하지 않은 경우
숨기고 상태 안내를 보여준다.

단, UI 숨김은 보안 수단이 아니다.

------------------------------------------------------------------------

## 16. 라이어는 정확히 1명

MVP 라이어 게임의 라이어는 항상 정확히 한 명이다.

다음을 만들지 않는다.

``` text
라이어 수 선택 UI
2명 라이어 모드
라이어 수 설정값
```

------------------------------------------------------------------------

## 17. 카테고리 하드코딩 금지

라이어 게임 카테고리는 Backend API에서 조회한다.

Frontend에:

``` ts
['음식', '장소', '직업']
```

처럼 실제 카테고리 목록을 하드코딩하지 않는다.

`RANDOM` 역시 API 계약을 따른다.

전체 제시어 목록을 Frontend에 포함하지 않는다.

------------------------------------------------------------------------

## 18. 비밀 정보

게임 종료 전 다른 Player의 역할을 절대 표시하지 않는다.

시민:

``` text
myRole = CITIZEN
keyword = <value>
```

라이어:

``` text
myRole = LIAR
keyword = null
```

라이어에게 keyword를 추론하거나 채워 넣지 않는다.

다른 Player 역할을 Frontend에 미리 내려받아 숨기는 방식으로 구현하지
않는다.

------------------------------------------------------------------------

## 19. 투표는 비밀투표

누가 누구에게 투표했는지는 공개하지 않는다.

투표 중에도 공개하지 않는다.

투표 종료 후에도 공개하지 않는다.

화면에서 표시 가능한 것은:

``` text
누가 투표를 완료했는지
현재 완료 인원
라운드 종료 후 후보별 총 득표수
최종 지목 결과
```

다음 UI를 만들지 않는다.

``` text
종윤 → 철수
예은 → 종윤
```

------------------------------------------------------------------------

## 20. 투표 진행 중 득표수 금지

전원이 투표하기 전에 다음을 표시하지 않는다.

``` text
현재 후보별 득표수
현재 1위
누가 누구를 찍었는지
```

투표가 완료된 뒤 서버가 제공한 최종 득표수만 표시한다.

------------------------------------------------------------------------

## 21. 자기 자신 투표 금지

현재 Player는 자신에게 투표할 수 없다.

서버의 `eligibleCandidates`를 기준으로 후보를 렌더링한다.

Frontend에서 전체 Player 목록을 기반으로 후보를 임의 계산하지 않는다.

------------------------------------------------------------------------

## 22. 재투표

동률이면 동률 후보만 대상으로 재투표한다.

재투표 횟수 제한은 없다.

단독 최다 득표자가 결정될 때까지 반복한다.

Frontend에서:

``` text
최대 2회
3회 이후 랜덤
```

같은 제한을 만들지 않는다.

서버가 제공한:

``` text
voteRound
eligibleCandidates
```

를 그대로 사용한다.

------------------------------------------------------------------------

## 23. 라이어 최종 추측

최종 지목자가 실제 라이어일 때만 진행된다.

실제 라이어:

``` text
정답 입력
제출
```

시민:

``` text
대기 UI
```

시민에게 입력창을 렌더링하지 않는다.

정답 판정은 Backend가 한다.

Frontend에서 문자열 비교로 승패를 결정하지 않는다.

------------------------------------------------------------------------

## 24. 역할 확인

본인의 역할 확인 여부만 행동 가능 여부에 사용한다.

이미 역할을 확인한 Player에게 다시 확인 버튼을 제공하지 않는다.

다른 Player에 대해서는 확인 완료 여부만 표시할 수 있다.

역할 자체는 표시하지 않는다.

------------------------------------------------------------------------

## 25. Host 권한

Host 전용 UI는 `me.host` 기준으로 렌더링한다.

대표 Host 행동:

``` text
게임 선택
GameSession 생성
게임 시작
투표 시작
게임 취소
장기 미접속 Player 제외
다음 게임 선택
```

Frontend에서 Host 여부를 보안 수단으로 사용하지 않는다.

Backend에서도 검증한다고 가정한다.

------------------------------------------------------------------------

## 26. API 계약 변경 금지

`API_SPEC.md`에 없는 Endpoint나 DTO를 편의상 임의 생성하지 않는다.

예를 들어 Frontend 구현이 편하다는 이유로:

``` text
GET /api/liar/current-role
GET /api/vote/result
```

같은 API가 필요하다고 가정하지 않는다.

계약 변경이 필요하면 먼저 변경 필요성을 보고한다.

------------------------------------------------------------------------

## 27. DTO 임의 변경 금지

API Response가 불편하더라도 임의로 필드명을 바꾸어 Backend 계약처럼
사용하지 않는다.

예:

``` text
API: gameSessionId
Frontend contract: sessionId
```

처럼 별도 계약을 만들지 않는다.

UI 내부 ViewModel 변환은 가능하지만 API DTO와 명확하게 구분한다.

------------------------------------------------------------------------

## 28. HTTP 호출

컴포넌트 곳곳에서 직접 `fetch()`를 호출하지 않는다.

공통 HTTP Client와 Feature API 모듈을 사용한다.

공통 처리:

``` text
Base URL
X-Client-Id
JSON
ApiError
Network Error
```

------------------------------------------------------------------------

## 29. Mutation 중복 실행 방지

다음 행동은 요청 중 버튼을 비활성화한다.

``` text
Room 생성
Room 참가
게임 시작
역할 확인
투표 시작
투표 제출
라이어 정답 제출
블라인드 정답 제출
```

더블 클릭으로 중복 요청을 발생시키지 않는다.

Backend의 멱등성/중복 방지와 별개로 Frontend에서도 UX를 보호한다.

------------------------------------------------------------------------

## 30. Optimistic Update 제한

다음 핵심 상태를 Optimistic하게 확정하지 않는다.

``` text
게임 시작
역할 확인 완료
투표 제출 결과
재투표
라이어 판정
정답 여부
승패
```

Backend 결과 또는 새 `/state`를 기다린다.

------------------------------------------------------------------------

## 31. WebSocket

WebSocket은 상태 변경 알림 수단이다.

WebSocket 이벤트만으로 전체 상태를 영구 재구성하지 않는다.

중요한 상태 변경 후에는 `room-state` Query를 invalidate하여 서버 상태와
동기화한다.

------------------------------------------------------------------------

## 32. WebSocket 이벤트 payload 확장 금지

Frontend가 필요하다는 이유로 비밀 데이터를 이벤트에 포함한다고 가정하지
않는다.

특히 `PLAYER_VOTED`에:

``` text
targetPlayerId
```

가 있다고 가정하지 않는다.

`API_SPEC.md`의 이벤트 계약을 따른다.

------------------------------------------------------------------------

## 33. 재접속

WebSocket 연결이 끊기면 자동 재연결을 시도한다.

재연결 성공:

``` text
GET /api/rooms/{roomId}/state
```

를 기준으로 화면을 복구한다.

과거 WebSocket 이벤트를 모두 재생하여 복원하는 구조를 만들지 않는다.

------------------------------------------------------------------------

## 34. 새로고침

새로고침 후 localStorage의 game state를 이용해 화면을 복원하지 않는다.

반드시 서버 state를 조회한다.

이미 투표한 Player에게 투표 UI가 다시 나타나지 않게 서버의
`myVoteSubmitted`를 사용한다.

------------------------------------------------------------------------

## 35. Polling 남용 금지

정상 상태에서:

``` text
GET /state every 1 second
```

같은 Polling을 구현하지 않는다.

기본 구조:

``` text
REST + WebSocket + state resync
```

WebSocket 장애 fallback이 필요할 경우 별도 판단한다.

------------------------------------------------------------------------

## 36. Mock 우선 개발 가능

Backend가 없어도 Frontend를 완성할 수 있도록 Mock 계층을 사용한다.

Mock은 실제 `API_SPEC.md`와 동일한 Request/Response/Event 구조를
사용한다.

Mock을 위해 Product API 계약을 변경하지 않는다.

------------------------------------------------------------------------

## 37. Mock 코드 격리

권장:

``` text
src/mocks/
```

Mock 분기를 화면 컴포넌트마다 넣지 않는다.

금지 예:

``` ts
if (useMock) {
  // mock
} else {
  // real
}
```

가 여러 View에 반복되는 구조.

API/Realtime Adapter 경계에서 교체한다.

------------------------------------------------------------------------

## 38. 필수 Mock 시나리오

최소한 다음 시나리오를 확인할 수 있어야 한다.

``` text
Room 생성
Room 참가
Host / 일반 Player 차이
시민 역할
라이어 역할
역할 확인 대기
Discussion
투표
투표 대기
정상 투표 결과
동률
재투표
재투표 반복
시민 오지목
라이어 검거
라이어 정답
라이어 오답
블라인드 게임 시작
블라인드 상대방 제시어 표시
블라인드 정답 / 오답 / 재시도
블라인드 승자 결정 및 종료 결과
연결 끊김
재접속
Host 변경
게임 종료 후 다음 게임
```

------------------------------------------------------------------------

## 39. TypeScript

`any`를 무분별하게 사용하지 않는다.

API DTO, WebSocket Event, Game State는 타입을 정의한다.

게임 상태는 가능한 경우 discriminated union으로 표현한다.

타입 오류를 `as any`로 숨기지 않는다.

------------------------------------------------------------------------

## 40. 컴포넌트 크기

하나의 거대한 `RoomPage.tsx` 또는 `LiarGame.tsx`에 전체 게임을 구현하지
않는다.

phase별 View를 분리한다.

단, 지나치게 작은 컴포넌트를 기계적으로 수십 개 만드는 것도 피한다.

화면 책임과 재사용성을 기준으로 분리한다.

------------------------------------------------------------------------

## 41. 비즈니스 로직 위치

Frontend에서 필요한 UI 로직은 Hook 또는 Feature 내부 함수로 분리할 수
있다.

하지만 Backend 도메인 규칙을 복제하지 않는다.

예:

허용:

``` text
현재 phase에 맞는 View 선택
버튼 disabled 여부
표시용 문자열 변환
```

금지:

``` text
투표 직접 집계
동률 후보 직접 결정
라이어 직접 선정
정답 직접 판정
승자 직접 결정
블라인드 정답 직접 판정
```

------------------------------------------------------------------------

## 42. 오류 처리

API 오류의 `code`를 기준으로 처리한다.

`message` 문자열 비교로 분기하지 않는다.

상태 충돌 오류가 발생하면 필요 시 `/state`를 다시 조회한다.

사용자에게 stack trace, raw exception, 개발용 메시지를 노출하지 않는다.

------------------------------------------------------------------------

## 43. 네트워크 오류

Backend 도메인 오류와 Network Error를 구분한다.

네트워크 단절 시:

``` text
연결이 불안정해요.
다시 연결하고 있습니다...
```

같은 상태를 보여줄 수 있다.

재연결 후 서버 상태를 다시 조회한다.

------------------------------------------------------------------------

## 44. 접근성

최소한 다음을 지킨다.

``` text
실제 button 요소
input label
키보드 focus
색상만으로 상태 표현 금지
충분한 대비
터치 가능한 크기
```

------------------------------------------------------------------------

## 45. 애니메이션

게임 재미를 높이는 짧은 연출은 허용한다.

예:

``` text
첫 발언자 공개
투표 결과
라이어 정체 공개
승패
```

애니메이션이 서버 상태보다 우선해서는 안 된다.

과도한 애니메이션으로 진행을 지연시키지 않는다.

------------------------------------------------------------------------

## 46. 효과음

효과음/BGM은 MVP 핵심 요구사항이 아니다.

SPEC에 별도 요청이 없다면 구현 우선순위를 높이지 않는다.

추가할 경우 음소거 가능해야 하며 게임 진행에 필요한 정보를 소리만으로
전달하지 않는다.

------------------------------------------------------------------------

## 47. 새 Dependency

Dependency 추가 전 확인:

1.  현재 Dependency로 해결 가능한가?
2.  작은 유틸 함수로 해결 가능한가?
3.  번들 크기에 비해 가치가 있는가?
4.  프로젝트 구조를 불필요하게 복잡하게 만들지 않는가?

단순 기능을 위해 대형 라이브러리를 추가하지 않는다.

------------------------------------------------------------------------

## 48. 코드 품질

다음을 지킨다.

``` text
명확한 이름
작은 책임
중복 최소화
불필요한 abstraction 금지
dead code 제거
console debug 제거
TypeScript 오류 없이 build
ESLint 오류 정리
```

사용하지 않는 미래 기능을 미리 구현하지 않는다.

------------------------------------------------------------------------

## 49. 구현 완료 후 확인

작업 완료 시 최소한 다음을 확인한다.

``` text
npm run build
```

프로젝트에 테스트가 구성되어 있다면 관련 테스트도 실행한다.

실패한 build/test가 있으면 숨기지 말고 원인과 남은 문제를 보고한다.

------------------------------------------------------------------------

## 50. SPEC 수정

구현 과정에서 SPEC이 현실적으로 맞지 않는 경우 임의로 코드를 SPEC과
다르게 만들지 않는다.

다음 형태로 보고한다.

``` text
현재 SPEC:
문제:
제안:
영향 범위:
```

승인 없이 공통 SPEC의 의미를 바꾸지 않는다.

------------------------------------------------------------------------

## 51. 금지 사항 요약

다음을 하지 않는다.

``` text
SPEC에 없는 기능 추가
Frontend에서 승패 계산
Frontend에서 투표 집계
Frontend에서 동률 후보 계산
Frontend에서 역할 선정
Frontend에서 제시어 선정
다른 Player 역할 노출
라이어에게 제시어 노출
누가 누구에게 투표했는지 노출
투표 중 현재 득표수 노출
블라인드 게임 종료 전 본인 제시어 노출
블라인드 카테고리 선택 / 공개
블라인드 정답과 승자를 Frontend에서 판정
재투표 횟수 제한 추가
동률 랜덤 지목 추가
카테고리 하드코딩
전체 제시어 Frontend 저장
Server State를 여러 Store에 복제
1초 Polling 남용
게임 phase별 URL 생성
거대한 단일 게임 컴포넌트
과도한 범용 게임 엔진
불필요한 Dependency 추가
```

------------------------------------------------------------------------

## 52. 최종 구현 기준

코드를 작성하기 전에 항상 다음 순서로 판단한다.

``` text
SPEC에 정의되어 있는가?
        ↓
현재 서버 상태는 무엇인가?
        ↓
현재 Player는 누구인가?
        ↓
현재 Player가 할 수 있는 행동은 무엇인가?
        ↓
그 행동에 필요한 UI만 보여준다.
```

NOOPI Frontend의 가장 중요한 원칙:

> **현재 Player가 지금 해야 할 것만 보여준다.**

그리고:

> **게임은 사람끼리, 진행은 웹이.**
