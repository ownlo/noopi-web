# NOOPI 백엔드 블라인드 게임 구현 프롬프트

NOOPI 백엔드 프로젝트에 새로운 게임 **「블라인드 게임」**을 실제 구현해줘.

구현을 시작하기 전에 반드시 다음을 먼저 수행해줘.

1. 저장소의 `AGENTS.md`를 읽어라.
2. 아래 SPEC 문서를 모두 읽어라.
   - `SERVICE_SPEC.md`
   - `GAME_SESSION_SPEC.md`
   - `REALTIME_SPEC.md`
   - `API_SPEC.md`
   - `games/BLIND_GAME_SPEC.md`
   - 프로젝트의 Backend Architecture 관련 문서
3. 기존 라이어 게임 구현 구조를 분석해 공통 패턴을 파악하라.
4. 현재 API 계약과 블라인드 게임 SPEC 사이에 충돌이 있다면 임의로 결정하지 말고 먼저 보고하라.

목표는 Frontend Mock으로만 동작하던 블라인드 게임을 실제 Backend 상태와 API로 플레이할 수 있게 만드는 것이다.

## 핵심 게임 규칙

- 블라인드 게임은 정확히 2명만 참여할 수 있다.
- 게임 시작 시 Backend가 두 Player에게 서로 다른 제시어를 배정한다.
- 각 Player는 자신의 제시어를 볼 수 없다.
- 각 Player에게는 상대방의 제시어만 제공한다.
- 상대방의 화면에 표시되는 제시어를 보며 서로 질문하고, 자신의 제시어를 추측한다.
- 정답 제출 횟수에는 제한이 없다.
- 오답이면 게임은 계속 진행되고 다시 제출할 수 있다.
- 먼저 자신의 제시어를 맞힌 Player가 승자가 된다.
- 정답 여부와 승자는 반드시 Backend가 판정한다.
- 게임 종료 전에는 자신의 제시어를 절대 노출하지 않는다.
- 게임 종료 후에는 승자와 양쪽 Player의 제시어를 결과로 제공한다.
- 동시에 제출되는 상황에서도 승자는 정확히 한 명만 확정되어야 한다.

## 상태 모델

기존 GameSession 구조와 규칙을 유지하면서 블라인드 게임 상태를 다음 phase로 표현해줘.

- `READY`
- `GUESSING`
- `FINISHED`
- `CANCELLED`

### READY

- GameSession이 생성됐지만 아직 게임을 시작하지 않은 상태
- Host만 게임을 시작할 수 있음
- 시작 시점에도 참가자가 정확히 2명인지 검증

### GUESSING

현재 Player 기준으로 다음 정보만 제공:

- 상대방 Player의 공개 가능한 기본 정보
  - `playerId`
  - `nickname`
- `opponentKeyword`
- 자신의 제시어는 응답에 포함하지 않음

절대 제공하면 안 되는 정보:

- `myKeyword`
- 전체 제시어 배정 목록
- 상대방의 정답
- 미리 결정된 승자 정보

### FINISHED

다음을 제공:

- `winnerPlayer`
  - `playerId`
  - `nickname`
- `keywordAssignments`
  - `playerId`
  - `nickname`
  - `keyword`

게임이 끝난 뒤에만 전체 제시어 배정을 공개한다.

## API

기존 `API_SPEC.md`의 URL, HTTP Method, DTO 이름과 오류 응답 형식을 그대로 따라라.

Frontend에서 필요한 행동은 다음과 같다.

1. `BLIND` 타입 GameSession 생성
2. Host가 게임 시작
3. 현재 Player가 자신의 제시어 추측 제출
4. Room state 조회 시 현재 Player 기준으로 안전하게 필터링된 블라인드 상태 반환

정답 제출 응답에는 최소한 다음 의미가 필요하다.

```json
{
  "correct": false
}
```

- 오답이면 `correct: false`
- phase는 `GUESSING` 유지
- 정답이면 `correct: true`
- GameSession은 `FINISHED`로 전환
- 승자는 제출한 Player로 확정

`API_SPEC.md`에 이미 정확한 DTO가 있다면 그 계약을 최우선으로 사용하고, 위 예시를 이유로 별도 계약을 만들지 마라.

## 동시성

정답 제출은 반드시 트랜잭션과 동시성 제어를 고려해 구현해줘.

두 Player가 거의 동시에 정답을 제출해도:

- 최초로 정상 처리된 정답 제출만 승자로 확정
- 이후 요청은 이미 종료된 GameSession 상태로 처리
- 승자가 덮어써지지 않음
- 게임 종료 이벤트가 중복 발행되지 않음

프로젝트에서 사용하는 기존 낙관적 락, 비관적 락, 원자적 상태 전이 방식이 있다면 해당 패턴을 따라라.

## 제시어

- 제시어 선택과 배정은 Backend가 담당한다.
- Frontend에 전체 제시어 목록을 전달하지 않는다.
- 두 Player에게 같은 제시어가 배정되지 않게 한다.
- 카테고리 선택 UI나 카테고리 공개 기능은 추가하지 않는다.
- 제시어 정규화와 정답 비교 방식은 SPEC을 따른다.
- SPEC에 정규화 규칙이 없다면 임의로 복잡한 유사 정답 판정을 추가하지 말고, 필요한 결정 사항을 보고하라.

## 권한과 검증

- GameSession 생성과 시작은 Host만 가능
- Room에 참가 중인 Player만 자신의 정답을 제출할 수 있음
- 정확히 2명이 아니면 블라인드 게임을 생성하거나 시작할 수 없음
- `FINISHED` 또는 `CANCELLED` 상태에서 정답 제출 불가
- 다른 Player를 대신해 답을 제출할 수 없음
- `clientId`를 실제 인증 토큰처럼 신뢰하지 말고 기존 Room 참여 검증 구조를 사용

## 실시간 이벤트

`REALTIME_SPEC.md`의 기존 WebSocket 이벤트 계약을 따라라.

필요한 상태 변화:

- GameSession 생성
- 게임 시작
- 오답 제출
- 게임 종료

WebSocket은 상태 변경 알림으로만 사용하고, 비밀 제시어를 이벤트 payload에 직접 포함하지 마라.

게임 시작과 종료처럼 Room state가 달라지는 시점에는 Frontend가 `/state`를 다시 조회할 수 있는 기존 이벤트를 사용한다.

오답 제출은 상대방에게 정답 문자열이 공개되지 않아야 한다. 오답 이벤트가 SPEC에 없다면 임의로 새로운 공개 payload를 만들지 말고, 이벤트 추가 필요성을 먼저 보고하라.

## 오류 처리

프로젝트의 기존 `ApiError` 형식을 사용하고 message 문자열이 아니라 안정적인 error code를 제공해줘.

최소 검증 대상:

- 블라인드 게임 인원 부족 또는 초과
- Host가 아닌 Player의 생성/시작 요청
- 존재하지 않는 GameSession
- 현재 Room에 속하지 않은 Player
- 잘못된 게임 phase
- 빈 정답
- 이미 종료된 게임에 대한 제출
- 중복 또는 동시 제출

오류 코드 이름은 기존 프로젝트 규칙과 `API_SPEC.md`를 우선한다.

## 구현 구조

- Room 도메인에 블라인드 게임 규칙을 직접 넣지 않는다.
- 기존 게임 Feature/Domain 구조에 맞춰 블라인드 게임 전용 영역으로 분리한다.
- 라이어 게임 코드를 복사해 거대한 조건문을 만들지 않는다.
- 불필요한 범용 게임 엔진이나 새 Dependency는 추가하지 않는다.
- Entity를 API 응답으로 직접 노출하지 않는다.
- Player별 state 응답을 만들 때 비밀 정보가 섞이지 않도록 명시적인 DTO 변환을 사용한다.

## 테스트

최소한 다음 테스트를 작성하고 실행해줘.

1. 정확히 2명일 때 GameSession 생성 성공
2. 1명 또는 3명 이상일 때 생성/시작 실패
3. Host만 게임 시작 가능
4. 게임 시작 시 서로 다른 제시어 배정
5. 각 Player state에 상대방 제시어만 노출
6. 게임 종료 전 자신의 제시어가 JSON 응답에 존재하지 않음
7. 오답 제출 시 `GUESSING` 유지 및 재시도 가능
8. 정답 제출 시 제출자가 승자로 확정
9. 종료 결과에 승자와 양쪽 제시어 포함
10. 종료 후 추가 제출 거부
11. 빈 정답 거부
12. Room에 속하지 않은 `clientId`의 제출 거부
13. 동시 정답 제출 시 승자가 한 명만 확정
14. 게임 종료 이벤트가 한 번만 발행
15. 재조회 및 재접속 시 서버 상태로 정상 복구
16. 라이어 게임과 기존 Room 기능에 회귀 오류가 없음

## 완료 조건

- 실제 Backend API로 블라인드 게임 전체 흐름이 동작
- Frontend Mock 전용 분기 없이 기존 API Adapter와 연결 가능
- 비밀 제시어 노출 없음
- 승자와 정답을 Frontend에서 계산할 필요 없음
- 동시 제출 안전성 확보
- 관련 테스트 전체 통과
- 프로젝트 전체 build/test 통과
- 변경된 API나 SPEC이 있다면 함께 문서화

구현 완료 후 다음을 보고해줘.

1. 변경한 파일
2. 구현한 상태 전이
3. API 및 DTO
4. 비밀 정보 보호 방식
5. 동시성 처리 방식
6. WebSocket 이벤트 처리
7. 실행한 테스트와 결과
8. Frontend에서 Mock을 끄고 연동할 때 필요한 설정
9. 남아 있는 문제 또는 SPEC 결정이 필요한 부분

특히 다음 두 가지를 가장 엄격하게 검증해줘.

> 각 사용자에게 상대방의 제시어만 내려주는 Player별 상태 변환

> 동시 정답 제출 시 단 한 명의 승자만 확정하는 원자적 상태 전이
